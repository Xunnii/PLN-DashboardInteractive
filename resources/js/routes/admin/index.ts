import displayImages from './display-images'
import displayVideos from './display-videos'
import kontainerBawah from './kontainer-bawah'
const admin = {
    displayImages: Object.assign(displayImages, displayImages),
displayVideos: Object.assign(displayVideos, displayVideos),
kontainerBawah: Object.assign(kontainerBawah, kontainerBawah),
}

export default admin